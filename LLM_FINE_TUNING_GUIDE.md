# 本地大模型微调执行手册（面向本项目）

> 适用场景：你负责“巡检报告生成 + 问答”这部分的大模型优化。  
> 目标：让模型输出更专业、更稳定、风格统一中文，并可直接接入本项目（Ollama）。

## 1. 微调目标拆解

结合当前项目功能（巡检报告、智能问答），建议拆成两条线：

1. **报告生成微调（主线）**
   - 输入：结构化巡检结果（材质、楼层、加层、隐患列表）
   - 输出：符合住建场景的标准中文巡检报告
2. **问答对齐微调（辅线）**
   - 输入：报告内容 + 用户问题
   - 输出：基于报告事实的回答（不乱编）

先做主线，主线稳定后再做辅线。

## 2. 数据准备（最关键）

你要做的是“高质量数据集”而不是先拼命跑训练。

### 2.1 数据来源建议

- 历史巡检记录（`inspection_records` + `defects`）
- 你们老师/专业方提供的报告模板
- 人工修订后的高质量报告（优先级最高）

### 2.2 训练样本格式（推荐）

建议统一成 `JSONL`，每行一个样本，使用 `messages` 结构：

```json
{"messages":[
  {"role":"system","content":"你是住建巡检报告助手，请按模板输出正式中文报告。"},
  {"role":"user","content":"巡检数据：材质=涂料；楼层=18层；加层=无加层；隐患=[{类型:裂缝,面积:120.5},{类型:渗水,面积:80.2}]。请输出巡检报告。"},
  {"role":"assistant","content":"（标准中文报告正文）"}
]}
```

### 2.3 样本数量建议（课程项目可落地）

- 第一版可用：`300 ~ 800` 条高质量样本
- 有明显提升：`1000+` 条
- 宁可少但准，不要大量低质量脏数据

## 3. 训练方案建议（本地可执行）

你们当前用的是 `qwen2:1.5b`，建议先做 **QLoRA**（成本低、成功率高）。

### 3.1 推荐技术路径

- 基座模型：Qwen2-1.5B-Instruct（或你们当前等价基座）
- 微调方式：LoRA / QLoRA
- 框架建议：LLaMA-Factory（上手快）或 Unsloth（更省显存）

### 3.2 典型训练参数（第一版）

- `lora_rank=16`
- `lora_alpha=32`
- `lora_dropout=0.05`
- `learning_rate=2e-4`
- `epoch=3`
- `max_seq_len=2048`
- `batch_size` 按显存调（如 `1~4`，配合梯度累积）

> 先跑小样本（50~100 条）验证流程，确保能收敛，再跑全量。

## 4. 评估与验收（必须有）

课程汇报里最加分的是“有对比、有指标”。

### 4.1 最小评估集

- 单独留 `100` 条不参与训练的数据作为验证/测试
- 分类覆盖：无隐患、单隐患、多隐患、复杂隐患

### 4.2 建议指标

- 报告结构完整率（是否包含模板要求字段）
- 术语规范率（是否使用标准中文术语）
- 幻觉率（是否编造不存在的缺陷）
- 人工评分（专业性、可读性、可执行性）

### 4.3 对比方式

- 基座模型 vs 微调模型（同一批输入）
- 做表格：输入摘要、原输出、微调后输出、人工评分

## 5. 接入当前项目（Ollama）

你们项目里已经通过环境变量控制模型名：

- `OLLAMA_BASE_URL`
- `OLLAMA_MODEL`

微调后你只要：

1. 导出模型（或合并权重）  
2. 在 Ollama 创建新模型（例如 `inspection-qwen:ft-v1`）  
3. 把 `.env` 里的 `OLLAMA_MODEL` 改成新模型名  
4. 跑 `app.py`/`api` 做端到端回归测试

## 6. 你接下来一周可执行的节奏

### Day 1-2

- 定稿报告模板（你提到会提供专业模板）
- 清洗并构建第一版训练集（先 300 条）

### Day 3

- 跑小样本 QLoRA（流程验证）
- 修复数据格式和提示词问题

### Day 4-5

- 跑全量训练
- 导出微调模型并接入 Ollama

### Day 6

- 做基座 vs 微调对比评估表
- 产出汇报图表（指标+案例）

### Day 7

- 项目联调 + 汇报彩排

## 7. 常见坑位提醒

- 不要把模型权重提交到 GitHub（你们已配置忽略）
- 训练集里“输入格式必须稳定”，否则模型学不稳
- 问答微调要明确“仅基于报告回答”，避免幻觉
- 先做 SFT，再考虑 DPO/偏好优化，别一开始就上复杂流程

## 8. 建议你现在先做的三件事

1. 先把“报告模板字段清单”定下来（固定格式）  
2. 我帮你写一个“从数据库导出微调 JSONL”的脚本  
3. 我再给你一套“训练配置文件模板（可直接跑）”

