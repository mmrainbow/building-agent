# 微调模型本地调用说明

本文档用于说明本项目中微调后的多模态大模型如何在本地调用、如何接入项目，以及其他开发者需要接收哪些文件。

## 1. 模型文件说明

本项目微调得到的是基于 `Qwen2.5-VL-3B-Instruct` 的建筑外立面巡检多模态模型。

当前推荐使用的是合并后的完整模型目录：

```text
outputs/qwen2_5_vl_3b_building_merged
```

该目录是由：

```text
Qwen2.5-VL-3B-Instruct 原始模型
+
LoRA 微调参数
```

合并得到的完整模型目录。

注意：不要只复制其中某一个 `.safetensors` 文件。完整模型目录中还包含模型配置、分词器、图像处理器、生成配置等文件，缺少任意关键文件都可能导致模型无法加载。

## 2. 本地需要准备的文件

如果其他开发者也想在本地使用微调后的模型，需要准备以下内容：

```text
项目代码
models/                              # CV 检测模型权重
outputs/qwen2_5_vl_3b_building_merged # 微调后合并模型
```

其中 `models/` 目录通常包含：

```text
models/material.pth
models/main_building.pt
models/outer_obj.pt
models/add_predict.pth
models/best.pt
```

`outputs/qwen2_5_vl_3b_building_merged` 不建议上传到 GitHub，应通过网盘、移动硬盘、NAS、HuggingFace 私有仓库或 ModelScope 私有仓库传输。

## 3. 安装依赖

在项目根目录执行：

```bat
pip install -r requirements.txt
```

本地微调模型调用额外依赖主要包括：

```text
transformers
accelerate
safetensors
Pillow
torch
torchvision
```

如果使用 GPU，建议确认 CUDA 和 PyTorch 是否可用：

```bat
python -c "import torch; print(torch.cuda.is_available()); print(torch.cuda.get_device_name(0) if torch.cuda.is_available() else 'CPU')"
```

## 4. 项目中的调用方式

本项目已经新增本地模型调用模块：

```text
llm/local_vl_model.py
```

巡检报告生成节点位于：

```text
agent/nodes.py
```

当前逻辑为：

```text
上传建筑图片
→ CV 模型识别材质、楼层、加层、隐患
→ 如果启用本地微调模型，则调用 Qwen2.5-VL 微调模型生成中文报告
→ 如果本地模型调用失败，则回退到原有 Ollama 调用
```

也就是说，本地模型主要负责将图片和结构化检测结果转化为更专业、更符合住建巡检场景的中文报告。

## 5. 环境变量配置

在 Windows CMD 中可以这样设置：

```bat
set LOCAL_VL_MODEL_ENABLED=true
set LOCAL_VL_MODEL_PATH=./outputs/qwen2_5_vl_3b_building_merged
set LOCAL_VL_DEVICE_MAP=auto
set LOCAL_VL_TORCH_DTYPE=float16
set LOCAL_VL_MAX_NEW_TOKENS=512
set LOCAL_VL_MAX_PIXELS=131072
```

在 PowerShell 中可以这样设置：

```powershell
$env:LOCAL_VL_MODEL_ENABLED="true"
$env:LOCAL_VL_MODEL_PATH="./outputs/qwen2_5_vl_3b_building_merged"
$env:LOCAL_VL_DEVICE_MAP="auto"
$env:LOCAL_VL_TORCH_DTYPE="float16"
$env:LOCAL_VL_MAX_NEW_TOKENS="512"
$env:LOCAL_VL_MAX_PIXELS="131072"
```

环境变量含义：

| 环境变量 | 作用 | 推荐值 |
| --- | --- | --- |
| `LOCAL_VL_MODEL_ENABLED` | 是否启用本地微调模型 | `true` |
| `LOCAL_VL_MODEL_PATH` | merged 模型目录路径 | `./outputs/qwen2_5_vl_3b_building_merged` |
| `LOCAL_VL_DEVICE_MAP` | 模型加载设备分配 | `auto` |
| `LOCAL_VL_TORCH_DTYPE` | 模型推理精度 | `float16` |
| `LOCAL_VL_MAX_NEW_TOKENS` | 最大生成文本长度 | `512` |
| `LOCAL_VL_MAX_PIXELS` | 输入图片最大像素限制 | `131072` |

如果显存不足，可以降低：

```bat
set LOCAL_VL_MAX_PIXELS=65536
set LOCAL_VL_MAX_NEW_TOKENS=256
```

## 6. 在项目中启动调用

配置好环境变量后，启动 Gradio 页面：

```bat
python app.py
```

之后在网页中上传建筑外立面图片，系统会自动执行：

```text
图像检测
→ 结构化结果生成
→ 本地微调模型生成巡检报告
→ 保存历史记录
```

如果本地模型加载失败，页面仍然会尝试使用原来的 Ollama 方案进行报告生成。

## 7. 使用 Swift 命令行单独调用模型

如果不通过项目，只想单独测试微调后的模型，可以使用：

```bat
cd /d D:\pycharm\py_project\agent
set IMAGE_MAX_TOKEN_NUM=512

swift infer --model ./outputs/qwen2_5_vl_3b_building_merged --infer_backend pt --torch_dtype float16 --max_pixels 131072 --max_new_tokens 512 --temperature 0
```

进入交互后，可以输入图片路径和问题，用于单独测试模型效果。

## 8. Python 代码调用示例

项目中可以直接调用：

```python
from llm.local_vl_model import generate_local_inspection_report

report = generate_local_inspection_report(
    image_path="test.jpg",
    material="Coating",
    floor="3层",
    has_extension="无加层",
    defects=[
        {"id": 1, "type": "渗水", "area": 54029.5},
    ],
)

print(report)
```

该函数会自动：

```text
加载本地 merged 模型
读取图片
构造住建巡检提示词
输入结构化检测结果
生成中文巡检报告
```

## 9. 与 Ollama 的区别

原项目中的 Ollama 调用主要用于通用问答或报告生成：

```text
services/chat_service.py
agent/nodes.py 中的回退逻辑
```

本地微调模型调用的是 HuggingFace/Transformers 格式模型：

```text
llm/local_vl_model.py
outputs/qwen2_5_vl_3b_building_merged
```

二者区别：

| 方案 | 优点 | 缺点 |
| --- | --- | --- |
| Ollama | 部署简单、调用方便 | 不包含本次 LoRA 微调结果 |
| 本地 merged 模型 | 包含课程项目微调成果，更贴合住建巡检 | 占用显存更高，启动较慢 |

## 10. 常见问题

### 10.1 模型目录很大是否正常？

正常。`qwen2_5_vl_3b_building_merged` 是完整模型目录，包含原始模型权重和 LoRA 微调效果，所以会达到数 GB。

### 10.2 是否可以只传 LoRA 文件？

可以，但对方还需要额外下载原始 `Qwen2.5-VL-3B-Instruct` 模型，并用 LoRA adapter 加载。为了方便课程项目演示，推荐直接传 merged 完整模型目录。

### 10.3 GitHub 是否需要上传模型？

不需要。大模型文件和训练数据不应上传 GitHub。

当前 `.gitignore` 已忽略：

```text
outputs/
ollama-test/
```

### 10.4 显存不足怎么办？

可以降低图片输入规模：

```bat
set LOCAL_VL_MAX_PIXELS=65536
set LOCAL_VL_MAX_NEW_TOKENS=256
```

如果仍然显存不足，可以暂时关闭本地模型：

```bat
set LOCAL_VL_MODEL_ENABLED=false
```

系统会回退到 Ollama 方案。

## 11. 推荐交付方式

给其他开发者时，推荐提供：

```text
1. GitHub 项目代码
2. models/ 下的 CV 权重
3. outputs/qwen2_5_vl_3b_building_merged 完整模型目录
4. 本文档：微调模型本地调用说明.md
```

对方拿到后，在项目根目录执行：

```bat
pip install -r requirements.txt
set LOCAL_VL_MODEL_ENABLED=true
set LOCAL_VL_MODEL_PATH=./outputs/qwen2_5_vl_3b_building_merged
python app.py
```

即可在本地网页中调用微调后的多模态模型。
